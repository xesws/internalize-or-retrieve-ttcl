You are writing ONE natural user message to a personal assistant for a memory benchmark.
Output STRICT JSON only — no prose, no code fences.

The user message must state this fact as the user themselves would say it in passing (first person, casual, 1-3 sentences):

{canonical}

Rules: the message must clearly convey the fact's CONTENT (a reader can recover the fact from the message alone); do not mention "memory", "note this down", or "remember that"; no questions.

JSON shape: {{"turn_text": "..."}}
