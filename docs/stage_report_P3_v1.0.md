# Stage Report P3 v1.0 — 主矩阵五臂 (G3)

日期 2026-08-21 · 汇报人: coding agent · 状态: **G3 验收三项全部达成, 五臂主矩阵完成; 已停, 等 JQ 裁决 (P4 是否启动 / 进入 P5 出表)**

## 结论

test split 全量 (4 用户、210 记忆、740 探针) 五臂主矩阵 (S1 all-RAG / S2 all-edit / S3 随机 / S4 oracle / S5 LLM router) 在单 A40 pod 上完成, 每臂 journal 完整 (800 条/臂), 记分卡与 type × store 失败矩阵产出, S5 同配置重跑漂移界 |Δcomposite| = 0.025。**排序: S4 0.639 > S5 0.612 > S1 0.510 > S2 0.401 > S3 0.388**。S5 达 oracle 的 95.8% (判据线 80%), 显著高于两个极端臂 (较 max(S1,S2) +0.102); S5>S4 **未在 N=210 复现** (P2 小矩阵的反超确认为噪声, 如实标注, 不作主张)。失败矩阵呈现论文需要的特征性签名: reference fact 进权重后召回塌方 (0.23–0.28 vs RAG 0.50–0.55), all-edit 的 supersede 后新值正确率仅 0.13 且旧值残留 0.174, transient 进 RAG 会在延迟探针上自信断言陈旧状态 (反转计分 0.2), 丢弃则干净 (1.0)。probe–elicit–compose 读路径 (P3 前置必做) 五臂共享, planner/judge prompt 冻结并打哈希。

## 验收逐条勾对 (handbook §5 P3 → G3)

| 验收项 | 结果 |
|---|---|
| 全臂 journal 完整 | ✅ 5 臂 × 4 用户 × 800 条 items.jsonl (740 探针 + 15 unrelated + 45 场景行), 重跑幂等验证过 |
| 记分卡与失败矩阵产出 | ✅ results/p3_scorecard.json (冻结矩阵口径 configs/scoring_v1.yaml 逐格聚合) |
| 漂移界已测 | ✅ S5 同配置重跑: composite 0.612 → 0.637, |Δ| = 0.025; 分轴 Δ: recall +0.007 / freshness +0.013 / locality +0.056 |

## 主表 (综合分与五轴, 冻结计分口径)

| 臂 | composite | recall | freshness | locality | unrelated | 旧值残留 | judge 自然度 | cap-hit | 中位长度 |
|---|---|---|---|---|---|---|---|---|---|
| S1 all-RAG | 0.510 | 0.495 | 0.395 | 0.639 | 1.000 | 0.043 | 0.76 | 0.6% | 14 |
| S2 all-edit | 0.401 | 0.283 | 0.447 | 0.472 | 1.000 | **0.174** | 0.59 | 0.6% | 12 |
| S3 random | 0.388 | 0.283 | 0.382 | 0.500 | 1.000 | 0.130 | 0.89 | 0.9% | 15 |
| S4 oracle | **0.639** | 0.500 | **0.724** | **0.694** | 1.000 | 0.043 | **0.91** | 0.3% | 14 |
| S5 router | 0.612 | 0.471 | 0.671 | 0.694 | 1.000 | 0.043 | 0.68 | 0.1% | 14 |

S5/S4 = 0.958; S5 − max(S1, S2) = +0.102。judge 自然度 (DeepSeek V4 Pro, 冻结 prompt `bec5d95094013fd5`, 276 行 0 失败): S4 最自然, S5 次低——S5 的错路由 (与隐藏标签一致率 184/210) 把部分 fact 送进了编辑, 场景里这些内容的引出生硬; 这是 router 准确率与读路径的耦合信号, 属 v1 utility router (P4) 的动机证据。

## 失败矩阵 (type × route 召回, 论文实证心脏; n 为 QA+场景行)

| type \ route | rag | edit | drop |
|---|---|---|---|
| belief | 0.42–0.59 (225) | 0.385–0.483 (406) | 0.062 (65) |
| fact | **0.504–0.554** (952) | **0.233–0.282** (213) | 0.014 (143) |
| transient | 0.733–0.769 (129) | 0.111 (27) | 0.0 (114) |

特征签名 (全臂一致): ① fact 的正确归宿是 RAG (进权重塌方 0.23); ② belief 两边相近但编辑侧在 freshness/near-miss 上占优 (belief×supersede_new: S4 编辑侧 0.783); ③ **transient 进 RAG 是最坏放置**——延迟反转计分 0.2 (检索让陈旧状态长期可断言), 编辑 0.9 (权重里自然衰减), 丢弃 1.0 (干净)。near-miss (n=18 对): S2 碰撞最重 0.472 < S1 0.639 < S4/S5 0.694。supersede_new (n=23): S1 0.739 / S4 0.783 / S5 0.696 / **S2 0.13**——all-edit 重编辑代价的直接测量。

## Gate 阈值敏感性 (dev 单次扫描, handbook §4.2; 已冻结 0.90)

| 阈值 | 0.75 | 0.80 | 0.85 | 0.90 |
|---|---|---|---|---|
| own-key 命中 | 1.000 | 1.000 | 1.000 | 1.000 |
| unrelated 误触发 | 0.933 | 0.667 | 0.267 | **0.000** |

预注册规则 (0.85 通过即保留, 否则取最大可行值) 判定 0.90: 0.85 会把 27% 的无关问题放进 codebook 通路。已写入 configs/default.yaml 并入冻结清单 (data/p3/freeze_v1.json, 12 项含 planner/judge/router/compose scaffold 哈希)。

## Paper note (JQ 裁决要求记入): 压力参数 v2.1 的部署对应物

论文必须正面论证 v2.1 不是针对 RAG 的定向设计: **distractor 建模真实部署中的环境文档噪声** (个人助手的检索库里永远混有非答案材料——其他来源、其他用户的参考文档), **eviction 建模用户记忆库的容量生命周期** (有限预算下最旧条目被淘汰是所有生产检索库的常态), 且**二者不共享预算**——噪声常驻 (现实里环境文档不会被"用户记忆预算"淘汰), 只有真实记忆受容量约束。这两个机制各自有独立的部署叙事, 参数全部披露, 并有 pressure-off 消融行 (P5) 兜底。

## 读路径 (probe–elicit–compose) 首战数据

planner (glm-5.3, 冻结哈希 `302afb4fdd7ec495`): test 18 场景 72 探针, 计数合规 100%, target-free 泄漏 0。gate 触发数随编辑量单调 (S2 74 / S3 42 / S5 17 / S4 8 / S1 0)。场景使用率绝对值仍低 (belief 场景 0.067–0.133, fact 场景 0.097–0.226)——主动调用仍是最弱环节, 但 judge 自然度较 P2 (0.25–0.50) 已全面上升 (0.59–0.91), 且五臂同路对比公平。eos 观察项 (JQ 指示): 全臂 cap-hit ≤ 0.9%, 中位长度 12–15, 无异常截断——§4.1 eos 监督的早停倾向未出现。

## 异常与修复

1. Judge 后处理三次空回复后按纪律报错中止一次 (思考型模型偶发空 completion); 处置 = 该行记 judge_failed=null 并继续 (已评 276 行最终 0 失败), 失败处置策略在脚本内固化。
2. gate_sweep spike 首次派发时文件未入库 (只提交了部分目录), 补提交后重跑——无数据影响。
3. 无其他异常; 主矩阵 20 流 + 漂移 4 流全部一次跑通, 无重跑。

## 成本与用量

GPU: 主矩阵 ~55 min + 漂移 ~15 min (A40, 含 24 次模型加载)。API: glm-5.3 累计 1440 调用 / 2.58M tokens (订阅); deepseek-v4-pro 累计 318 调用 / 326K tokens ≈ $0.7, **余额 $6.56, 未过半** (P2 后 JQ 充值至 $7.28, 本轮消耗 ~$0.72)。

## 下一步 (等 JQ 裁决)

1. **P4 (utility router / S6) 是否启动**: 判据材料已备 (S5 错路由的失败签名 + dev 30–40 条双路对照可行); handbook 时间线触及 8/27 即砍。不启动则直接 P5。
2. **P5 出表**: 主表/失败矩阵/pressure-off 消融行/敏感性曲线/漂移界, 冻结记分卡 (summary 级允许入 git), 交写作。
3. 消融 (时间富余时): text-injection vs 参数化引出、lexical vs LLM planner (dev 子集)。

## 待 JQ 决策清单

1. P4 启动与否 (上面下一步 1)。
2. 场景使用率偏低是否作为 limitation 如实写 (我的判断: 是——它是 honest boundary, 且 judge 自然度与失败矩阵已足以支撑主张), 还是在 P5 前再做一轮读路径调优 (有超时间表风险)。
3. S2 的 supersede_new = 0.13 极端值: 建议论文按"重编辑代价"呈现并给出每链案例; 若你想要机制级解释 (gate 阈值 vs key 碰撞分解), 需要一个小型分析 run, 请裁决是否值得花。
