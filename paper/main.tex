% MEMPLACE — NeurIPS 2026 TTCL Workshop submission (general track, 4-9 pages)
% Double-blind: no author info anywhere. System referred to as MEMPLACE only.
\documentclass{article}
\usepackage[final,nonatbib]{neurips_2026} % TODO: swap to the official 2026 style file when released
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage[hidelinks]{hyperref}
\usepackage{url}

\title{Internalize or Retrieve: Online Memory Placement for Test-Time Continual Learning Agents}

\author{Anonymous}

\begin{document}
\maketitle

\begin{abstract}
% Abstract per proposal v1.1 with the JQ-mandated negative-result ending
% (p5_checklist v1.1, guideline 8): keep the first ~180 words of the
% proposal draft, REPLACE the final "can be learned" sentence with the
% negative-result framing: per-memory counterfactual utility regression does
% not recover oracle placement (the signal lives on the lifecycle axis),
% motivating sequence-aware placement learning as future work. Three [X]s
% filled from the frozen scorecard via paper_tables.py — never hand-copied.
\end{abstract}

\section{Introduction}
% 1 page. Thesis: the placement decision itself is the missing component
% between memory agents (all external) and knowledge editing (all internal).
% Dual-claim structure (p5_checklist v1.2): (i) conditional benefit — under
% retrieval pressure the router matches oracle placement and dominates both
% extremes and dual-write; (ii) the lifecycle signal — pressure-off ablation,
% S6 negative result, S7 global degradation, three pieces of evidence
% converging on the lifecycle axis as where placement value lives.

\section{MEMPLACE: System Overview}
% 1.5 pages. Write path (typed routing, dedup/supersede lifecycle, multi-key
% codebook edits), read path (probe-elicit-compose), serving (shadow
% editing). C3 SCOPE (p5_checklist v1.2): attribution is empirically
% supported; hot-swap is ARCHITECTURE DESCRIPTION ONLY.

\section{Experimental Setup}
% 1 page. Self-generated workload (disclosed per proposal v1.1 section 5.2;
% PersonaChat/MSC/LOCOMO as structural references only), five+two arms,
% frozen scoring semantics, pressure v2.1 deployment counterparts (see
% checklist), contamination discipline (target-free probes, frozen prompts).

\section{Results}
% 2 pages. Tables auto-generated by scripts/paper_tables.py into tables.tex.
% Main table + failure matrix + supersede attribution + misroute confusion +
% drift bound + pressure-off + S7 decomposition. Judged naturalness always
% paired with usage (guideline 4). S5-vs-S4 wording: within run-to-run
% variance (guideline 1).

\section{Related Work}
% 0.5 page. Four lines per proposal 3.4: editing / in-context editing /
% memory agents / learned memory management (Memory-R1 differentiation) +
% read-side routing orthogonality (Self-RAG, Adaptive-RAG).

\section{Limitations and Future Work}
% 0.5 page. Scenario usage (planner-probe-to-key distance framing, guideline
% on read-path diagnosis); transient wording per guideline 3 (trigger rate
% 0.37-0.40 vs 10\% assertion, no mechanism claims); sequence-aware placement
% learning (negative-result ending, guideline 8).

\bibliography{refs}

\appendix
\input{appendix}

\end{document}
